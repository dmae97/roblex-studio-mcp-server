import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
/**
 * Prompt for finding bugs and suggesting improvements in Roblex scripts
 */
export declare const bugFinder: {
    register: (server: McpServer) => void;
};
