import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
/**
 * Prompt for generating Roblex scripts with AI assistance
 */
export declare const scriptGenerator: {
    register: (server: McpServer) => void;
};
