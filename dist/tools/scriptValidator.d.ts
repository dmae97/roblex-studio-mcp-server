import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
/**
 * Tool for validating Roblex scripts for syntax errors and best practices
 */
export declare const scriptValidator: {
    register: (server: McpServer) => void;
};
