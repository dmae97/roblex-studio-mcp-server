import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
/**
 * Tool for creating and configuring physics systems in Roblex
 */
export declare const physicsSystem: {
    register: (server: McpServer) => void;
};
